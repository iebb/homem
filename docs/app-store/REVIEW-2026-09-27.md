# September 27, 2026 build update

At the owner's request, removed the AI data-sharing screens and their consent enforcement from the app and share extension. Sign-in now opens the workspace directly; chat, uploads, and connections use the existing authenticated server flow. Removed consent-only provider polling, Keychain receipts, localized copy, and tests. The general privacy-policy link remains in Settings.

The subtitle corrections from September 26 are retained in all four languages. App Store Connect review notes retain the owner's explanation:

> 5.1.1(i) / 5.1.2(i): The app does not send user data to a third-party AI service and does not include a third-party AI service. AI service can be linked by user inside their own cloud computer, which is unrelated to us; We do not operate it.

The public privacy policy at https://docs.kitta.co/homem/ states that Kitta does not collect user data through Homem and explains direct connections to user-selected servers. Removed references to the deleted consent UI. Published from `kitta-co/privacy-policy` commit `43b7ed6`.

Validation: all 114 unit/integration tests and the custom-server sign-in UI test passed on iPhone 17 Pro (iOS 26.5). Localization coverage passed for all 731 strings in Chinese, Spanish, and Japanese. The replacement Xcode Cloud build and submission are next. Manual release and existing reviewer login/contact details are retained.
