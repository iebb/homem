# App Review corrections — September 26, 2026

Applies to rejection `77599781-811b-4b88-b958-85c3a49c7238` (version 1.0, build 32).

- Removed Memoh from the subtitle in English, Simplified Chinese, Spanish, and Japanese. Source: `metadata.json`; saved in App Store Connect.
- Added a localized consent screen after sign-in and before connected workspace features. It describes messages/history, attachments/photos, recordings, instructions, memories, tool inputs/results, and remote workspace input, names the selected server and its configured recipients, and links to the privacy policy.
- Allow and decline are explicit choices. Permission is stored in Keychain for the account/workspace, disclosure version, and recipient configuration. HTTP mutations, streaming operations, chat sends, and uploads check permission; discovery failures block sharing. Provider or routing changes require renewed permission. The share extension follows the same rules.
- Settings → AI data sharing provides withdrawal and disconnect. Withdrawal blocks future client sharing; the screen explains that existing server tasks/data are not stopped/deleted.
- Published the revised policy at https://docs.kitta.co/homem/ from `kitta-co/privacy-policy` commit `5bf9721`. The custom review server is treated as a user-operated server.

Validation: simulator build; 121 unit/integration tests and the iPhone consent UI flow passed. The UI flow checks decline, reconnect, explicit permission, connected navigation, and withdrawal. Localization coverage passed for all 748 strings in Chinese, Spanish, and Japanese. The same consent UI flow and all seven privacy gate tests also passed on iPad Air 11-inch (M3). Xcode Cloud build 57 is archiving source commit `f4518fd`.

Submission is pending the corrected processed build. Existing manual release selection is preserved. The existing review credentials and contact information were verified visually and retained. App Store Connect renders those saved values as placeholders, so an empty DOM value does not mean that the saved field is missing. Review notes describe both corrections and the consent verification steps.
